export default (root, props) =>   /*css*/`
    ${root} { 
        display:flex;
        flex-wrap: wrap;
        width:100%;
    }  

    ${root} .ctx-options {
        display:flex;
        width:100%;
        padding-top: 1rem
    }
`
